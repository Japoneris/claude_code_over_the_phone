# Docker Container Manager - Streamlit App

App to collect info from the containers.
Help to download files/folders.

Build it with docker or locally.

### Docker

```bash
docker compose build
docker compose up -d
```

### Locally

```sh
python3 -m venv venv
source venv/bin/activate
pip3 install -r requirements.txt
streamlit run app.py
```


