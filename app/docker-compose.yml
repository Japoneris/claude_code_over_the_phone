version: '3.8'

services:
  streamlit-app:
    build: .
    container_name: streamlit-manager
    ports:
      - "8501:8501"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
