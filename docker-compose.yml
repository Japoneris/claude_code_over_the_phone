version: '3.8'

services:
  web-terminal:
    build: .
    container_name: web-terminal
    ports:
      - "7681:7681"
    restart: unless-stopped
    stdin_open: true
    tty: true
    env_file:
      - .env
    environment:
      - TERM=xterm-256color
